package runtime

const (
	// SDKVersion identifies the Go helper/runtime release shipped in this
	// Developer Kit. It evolves independently from plugin implementation versions.
	SDKVersion = "1.3.2"
)
